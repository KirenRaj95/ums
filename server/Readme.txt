In the backend ,

first npm i (to install the node modules),
next create a database called ums in pgadmin4,then
create a .env file in the  main server directory and paste this 
DB_USER=postgres
DB_HOST=localhost
DB_NAME=ums
DB_PASSWORD=******(your pgadmin4 password)
DB_PORT=5432
